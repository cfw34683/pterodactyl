# pterodactyl
3 scripts to help automate pterodactyl install and updates

These need to be run as root. There is options at the start to setup hostnames and a user.
These scripts assume this is a fresh install with no user other than root.
